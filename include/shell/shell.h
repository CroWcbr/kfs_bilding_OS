#ifndef __CROW__SHELL_H
#define __CROW__SHELL_H

namespace crowos
{
	namespace shell
	{

		class Shell
        {
        private:

        public:
            static void exec_cmd(char *command);
        };

	} // namespace shell

} // namespace crowos

#endif
